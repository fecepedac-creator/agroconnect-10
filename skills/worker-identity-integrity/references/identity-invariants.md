# Identity Invariants

1. `worker_usernames/{rut}` is unique by canonical RUT.
2. Mapping update requires proof of ownership or server-side authority.
3. Worker profile role is immutable from client.
4. Recovery workflows must be auditable.