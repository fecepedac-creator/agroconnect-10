# Isolated emulator-only values. Never use these placeholders in a deployed project.
SUPERADMIN_EMAILS=superadmin@mundoconnect.test
GEMINI_API_KEY=emulator-not-configured
GMAIL_APP_PASSWORD=emulator-not-configured
